package javastudy;

/*
한정된 값만 갖는 데이터 타입을 열거타입이라고 한다. 열거 타입의 이름을 정하고 소스 파일을 생성 후 사용.
    name()메소드 : 열거 객체가 가지고 있는 문자열을 리턴한다.
    ordinal()메소드 : 전체 열거 객체 중 몇번째 객체인지 알려준다. 순서는 0번부터 시작.
    compareTo()메소드 : 매개값으로 주어진 객체를 기준으로 전후로 볓 번재 위치하는지를 비교.
        알아보려는 값.compareTo(매개값)
    valueOf()메소드 : 매개값으로 주어진 문자열과 동일한 문자열을 가지는 객체를 리턴한다.
    values()메소드 : 열거 타입의 모든 객체들을 배열로 만들어 리턴한다.
 */
public class Enumeration {
}
